apiVersion: v1
kind: Service
metadata:
  name: {{ template "simplefeedreader.fullname" . }}
  labels:
    app: {{ template "simplefeedreader.name" . }}
    chart: {{ template "simplefeedreader.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "simplefeedreader.name" . }}
    release: {{ .Release.Name }}
